## 一、项目概述

随着新能源技术与物联网技术的快速发展，太阳能照明设备逐步向智能化、自动化、远程可视化方向升级。本项目以**树莓派**为核心控制单元，结合传感器检测、步进电机驱动、天文算法计算、串口通信与 MySQL 数据存储技术，搭建一套集人体感应、太阳自动追踪、设备状态远程上报于一体的智能太阳能路灯系统。

系统主要实现四大核心能力：利用人体红外传感器完成行人检测并通过串口向上位机传输状态信息；通过天文算法实时计算太阳高度角与方位角；驱动步进电机完成太阳能板角度自适应追踪，提升光能利用率；周期性采集设备运行数据、太阳位置数据并上传至远程 MySQL 数据库，实现设备状态云端监控。整套系统基于 Python 语言开发，部署于树莓派硬件平台，架构轻量化、部署简单、运行稳定，适用于户外智能照明、小型光伏发电场景。

## 二、运行环境说明

### 2.1 硬件环境

本项目主控硬件采用树莓派（推荐 3B+、4B 型号），搭配多种外设完成功能拓展。外设包含 PIR 人体红外传感器，用于感应行人活动；两路四相步进电机及驱动模块，分别控制太阳能板方位角与俯仰角，实现太阳追踪功能；硬件串口模块，完成树莓派与上位机的数据交互；设备需接入网络，保证能够正常访问远程 MySQL 服务器，实现数据远程上传。所有硬件通过树莓派 GPIO 引脚完成接线，依托 GPIO 口实现数字信号输入与输出控制。

### 2.2 软件环境

系统运行在 Raspberry Pi OS（Raspbian）操作系统之上，开发与运行语言为 Python 3.7 及以上版本。项目依赖多个第三方功能库，其中`RPi.GPIO`用于树莓派 GPIO 引脚读写与硬件控制，`pyserial`实现串口数据收发，`pymysql`完成 MySQL 数据库连接与数据操作，数学计算相关功能使用 Python 内置`math`库实现。

部署前需执行依赖安装命令：

bash



运行







```
pip install RPi.GPIO pyserial pymysql
```

项目整体无复杂编译流程，安装依赖后即可直接运行各功能脚本。

## 三、系统整体架构与功能模块

本系统采用模块化设计，按照功能划分为**人体感应与串口通信模块、太阳位置计算模块、步进电机追光控制模块、云端数据上报模块**四大独立模块，各模块职责清晰、相互配合，整体耦合度低，便于后期功能迭代与维护。

### 3.1 人体感应与串口通信模块（light.py）

该模块负责行人检测与串口数据交互，是路灯智能启停的感知单元。程序通过读取 GPIO 引脚电平判断 PIR 红外传感器状态，当检测到行人靠近时，自动通过硬件串口向上位机发送提示信息；同时增加状态锁机制，避免同一触发场景下重复发送数据，有效降低串口通信冗余。

程序具备完善的资源管理逻辑，当用户按下`Ctrl+C`终止程序时，会自动关闭串口、清空 GPIO 引脚状态，防止硬件资源占用与损坏。模块支持自定义红外引脚、串口端口、波特率与发送内容，参数配置灵活，可适配不同接线方式与上位机通信标准。

### 3.2 太阳位置计算模块（solar_calc.py）

本模块为整个追光系统提供核心数据支撑，基于天文算法结合当地经纬度、UTC 标准时间，精准计算实时太阳高度角与方位角。程序内置目标区域经纬度参数，通过历法换算、太阳黄经、赤纬、时角等一系列运算，解算出太阳在空间中的角度信息。

模块对外封装两个核心函数，`calculate_sun_position`为通用计算函数，可传入自定义时间、经纬度完成计算；`get_current_sun_position`为简化调用接口，直接获取当前时刻太阳角度数据，可供追光模块、数据上报模块直接调用。该模块纯算法实现，不依赖硬件，计算结果稳定可靠。

### 3.3 步进电机太阳追踪模块（catch_sun.py）

该模块为系统执行单元，根据预设目标角度驱动两路步进电机，完成太阳能板角度调节。其中 X 轴电机控制水平方位角（0°~360° 循环转动），Y 轴电机控制垂直俯仰角（-45°~45°）。程序内置电机步距、单步延时、角度容错值等参数，可有效规避电机频繁启停问题。

在角度调节逻辑上，程序会自动计算当前角度与目标角度的差值，判断电机最短转动方向，减少机械损耗。当角度偏差小于设定容错值时，电机停止动作。程序启动时统一初始化所有 GPIO 引脚，退出时复位引脚并执行 GPIO 资源清理，保障硬件长期稳定运行。

### 3.4 云端数据上报模块（datahouse.py、light_data.py）

两个文件功能基本一致，`light_data.py`为数据库字段适配优化版本，推荐优先使用。模块主要负责设备初始化与定时数据上传，程序启动后首先连接远程 MySQL 数据库，校验设备编号是否存在，若数据库无当前设备记录，则自动创建设备基础信息。

运行过程中，模块每小时执行一次数据采集与上报：调用太阳计算模块获取太阳角度数据，随机模拟太阳能电池剩余电量，将设备在线状态、电池电量、太阳高度角、太阳方位角、更新时间等信息更新至远程数据库。模块加入完整异常捕获机制，数据库连接失败、数据写入异常等场景均会打印日志，不造成程序崩溃，提升系统容错能力。

## 四、数据库设计说明

系统采用远程 MySQL 数据库实现设备数据持久化存储，数据库名称为`solor`，核心数据表为`lamps`，用于存储所有智能路灯设备信息与实时运行数据，表内字段及作用如下：

light_id 为设备唯一编号，作为数据表主键；byname 存储设备名称；area 记录设备部署区域；x、y 分别对应设备安装地点的经度与纬度；status 标识设备在线状态；lamp_status 表示路灯开关状态；mode 记录设备当前运行模式；battray_pack_one_charge 存储太阳能电池剩余电量；current_high_angel、current_direction_angel 分别记录实时太阳高度角与方位角；location_time 为数据最后更新时间；duration 标记设备运行时长；deleted 为软删除字段，用于数据管理。

数据表结构简单实用，能够满足设备状态监控、历史数据追溯、批量设备管理等基础物联网应用需求。

## 五、系统运行流程

系统整体运行流程分为独立运行与联动运行两种模式。单独运行各脚本时，可分别测试传感器检测、太阳计算、电机追光、数据上报单项功能；整套联动运行时，工作流程如下：首先启动数据上报程序，完成设备数据库注册并开启定时数据上传；随后运行太阳追踪程序，驱动电机将太阳能板调整至目标角度；最后启动人体感应程序，实时监测行人状态并与上位机通信。

所有程序均可通过`Ctrl+C`手动终止，终止后自动释放串口、GPIO 等硬件资源，保证设备安全。正常运行状态下，各模块各司其职，实现感知、计算、执行、上报全流程自动化。

## 六、系统特点与优化方向

### 6.1 系统优势

第一，模块化设计，各功能脚本独立运行、互不干扰，调试与维护难度低；第二，硬件兼容性强，基于通用树莓派与常规传感器、电机模块搭建，硬件成本低、采购方便；第三，具备完善的资源释放与异常处理机制，长时间户外运行稳定性高；第四，数据云端存储，实现设备远程监控，满足物联网基础管理需求。

### 6.2 后续优化方向

当前太阳追踪为固定目标角度的开环控制，后续可结合传感器实时太阳角度，实现**闭环自动追光**；目前电池电量为模拟数据，可接入电压检测模块采集真实电池参数；同时可增加路灯自动开关逻辑，结合人体感应与光照强度实现真正意义上的全自动智能路灯。

## 七、总结

本项目基于树莓派与 Python 技术，完成了智能太阳能路灯系统的设计与实现，整合了传感器检测、电机控制、天文运算、串口通信、数据库存储多项技术。系统功能完整、架构简洁、部署便捷，既实现了太阳能板自动追光以提升发电效率，又完成了行人智能感应与设备数据云端监控，符合户外新能源照明设备智能化的发展需求。整套方案不仅可作为嵌入式 Python 开发、物联网应用的实践案例，也可经过简单改造后落地于小型园区、道路照明等实际应用场景。